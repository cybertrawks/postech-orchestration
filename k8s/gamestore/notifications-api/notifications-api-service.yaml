apiVersion: v1
kind: Service
metadata:
  name: notifications-api
  namespace: gamestore
  labels:
    app: notifications-api
spec:
  type: ClusterIP
  selector:
    app: notifications-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
