apiVersion: v1
kind: Service
metadata:
  name: catalog-api
  namespace: gamestore
  labels:
    app: catalog-api
spec:
  type: ClusterIP
  selector:
    app: catalog-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
