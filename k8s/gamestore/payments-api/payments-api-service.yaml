apiVersion: v1
kind: Service
metadata:
  name: payments-api
  namespace: gamestore
  labels:
    app: payments-api
spec:
  type: ClusterIP
  selector:
    app: payments-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
