apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: gamestore
  labels:
    app: users-api
spec:
  type: ClusterIP
  selector:
    app: users-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
