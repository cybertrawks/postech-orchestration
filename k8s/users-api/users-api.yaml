apiVersion: v1
kind: ConfigMap
metadata:
  name: users-api-config
  namespace: gamestore
data:
  ASPNETCORE_ENVIRONMENT: "Production"
  ASPNETCORE_URLS: "http://+:8080"
  RabbitMQ__Host: "rabbitmq.infrastructure.svc.cluster.local"
  RabbitMQ__Port: "5672"
  RabbitMQ__VirtualHost: "/"
  JwtSettings__Issuer: "Postech.API"
  JwtSettings__Audience: "Postech.Client"
  JwtSettings__ExpirationMinutes: "60"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: gamestore
  labels:
    app: users-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
    spec:
      containers:
        - name: users-api
          image: ${DOCKER_USER}/users-api:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: users-api-config
            - secretRef:
                name: users-api-secret
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "192Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: gamestore
  labels:
    app: users-api
spec:
  type: NodePort
  selector:
    app: users-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30081
